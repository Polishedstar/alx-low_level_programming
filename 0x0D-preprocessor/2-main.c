#include <stdio.h>

/**
 * main - some program.compilitation take into
 * consideration the main.c file before compiling.
 * Return: void
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
